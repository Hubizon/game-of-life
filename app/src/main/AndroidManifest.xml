<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hubert.gameoflife">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher3_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/MY_ADMOB_APP_ID" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Shop.BuyActivity"
            android:label="Shop"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".House.ComputerActivity"
            android:label="House"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Work.ChooseJobActivity"
            android:label="Choose Job"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".Credits"
            android:label="Credits"/>
        <activity android:name=".Education.SkillsActivity"
            android:label="Skills"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>